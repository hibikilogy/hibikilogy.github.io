[package]
name = "hibikilogy-tools"
version = "0.1.0"
edition = "2021"

[lib]
path = "scripts/rust/lib.rs"

[[bin]]
name = "title-font-subset"
path = "scripts/rust/bin/title_font_subset/main.rs"
required-features = [ "font-tools" ]

[[bin]]
name = "site-artifact-rewrite"
path = "scripts/rust/bin/site_artifact_rewrite/main.rs"
required-features = [ "artifact-rewrite" ]

[[bin]]
name = "article-short-links"
path = "scripts/rust/bin/article_short_links/main.rs"

[[bin]]
name = "body-font-subset"
path = "scripts/rust/bin/body_font_subset/main.rs"
required-features = [ "font-tools" ]

[[bin]]
name = "deploy-markdown"
path = "scripts/rust/bin/deploy_markdown/main.rs"

[[test]]
name = "cli_smoke"
path = "scripts/rust/integration/cli_smoke.rs"
required-features = [
  "font-tools",
  "artifact-rewrite"
]

[features]
default = []
font-tools = [
  "dep:rayon",
  "dep:skera",
  "dep:walkdir",
  "dep:woofwoof",
  "dep:write-fonts"
]
artifact-rewrite = [
  "dep:base64",
  "dep:globset",
  "dep:image",
  "dep:lol_html",
  "dep:url",
  "dep:walkdir"
]

[dependencies]
anyhow = "1"
base64 = { version = "0.22", optional = true }
clap = { version = "4", features = [ "derive" ] }
globset = { version = "0.4", optional = true }
image = { version = "0.25", optional = true, default-features = false, features = [
  "bmp",
  "gif",
  "ico",
  "jpeg",
  "png",
  "tiff",
  "webp"
] }
lol_html = { version = "3", optional = true }
percent-encoding = "2"
pulldown-cmark = { version = "0.13", default-features = false }
rayon = { version = "1", optional = true }
serde = { version = "1", features = [ "derive" ] }
serde_json = "1"
sha2 = "0.10"
skera = { version = "0.3", optional = true }
chrono = { version = "0.4", default-features = false, features = [ "std" ] }
toml = "0.8"
toml_edit = "0.22"
url = { version = "2", optional = true }
walkdir = { version = "2", optional = true }
woofwoof = { version = "1", optional = true }
write-fonts = { version = "0.49", optional = true }

[dev-dependencies]
tempfile = "3"
